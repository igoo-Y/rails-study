 <h2>
    <%= @post.title %>
  </h2>

  <p>
    <%= @post.content %>
  </p>

  <span>
      <%= link_to '수정', (@board.present? ? edit_board_post_path(@board)  : edit_post_path(@post)) %> |
      <%= link_to '삭제', (@board.present? ? [@post.board, @post] : @post), method: :delete, data: { confirm: '게시물을 삭제하시겠습니까?' }%>
  </span>